// Feature flags for the application
// Toggle features on/off here

export const FEATURE_FLAGS = {
  /** Enable the Notes feature (floating panel, indicators, element selection) */
  NOTES_ENABLED: false,
} as const
